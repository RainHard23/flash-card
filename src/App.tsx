import { Button } from '@/components/ui/button'
import { TextField } from '@/components/ui/text-field'

export function App() {
  return (
    <div>
      <TextField type={'password'} />
      <Button variant={'icon'}>Батон</Button>
    </div>
  )
}
